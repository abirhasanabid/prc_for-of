/**
 * Given an array of strings, use a for...of loop to print each element of the array on a new line.
 */

const names = ["abid","rifat","hridoy","bristy"];

for(const name of names){
    console.log(name);
}